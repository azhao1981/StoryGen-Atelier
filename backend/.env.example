PORT=3005
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_TEXT_MODEL=gemini-3-pro-preview
GEMINI_IMAGE_MODEL=gemini-3-pro-image-preview

# Gemini API Custom Endpoint (Optional)
# Uncomment and set to use custom endpoint instead of default Google endpoint
# GEMINI_BASE_URL=https://your-custom-endpoint.com

# Gemini API Custom Headers (Optional)
# JSON format for additional HTTP headers
# GEMINI_CUSTOM_HEADERS={"User-Agent": "StoryGen-Atelier/1.0", "X-Custom-Header": "value"}

# AIHubMix API (用于视频生成 - 替代 Vertex AI)
# 如果设置此密钥，系统将优先使用 aihubmix.com API 进行视频生成
# 如果没有设置但设置了 GEMINI_API_KEY，同时没有 VERTEX_PROJECT_ID，也会使用此 API
AIHUBMIX_API_KEY=your_aihubmix_api_key_here

# Vertex AI (用于视频生成 - Veo 模型)
# 如果设置了 VERTEX_PROJECT_ID，系统将使用 Vertex AI 进行视频生成
# 注意：Vertex AI 不使用 API Key，而是使用 Google Cloud 服务账号认证
# 需要设置环境变量 GOOGLE_APPLICATION_CREDENTIALS="/path/to/service-account-key.json"
VERTEX_PROJECT_ID=your-gcp-project-id
VERTEX_LOCATION=us-central1
VERTEX_VEO_MODEL=veo-3.1-generate-preview

# API 选择逻辑：
# 1. 如果设置了 AIHUBMIX_API_KEY，优先使用 AIHubMix API
# 2. 如果没有 AIHUBMIX_API_KEY，但设置了 VERTEX_PROJECT_ID，使用 Vertex AI
# 3. 如果只有 GEMINI_API_KEY（且没有 VERTEX_PROJECT_ID），使用 AIHubMix API（用 GEMINI_API_KEY 作为密钥）
